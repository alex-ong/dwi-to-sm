[project]
name = "dwi-to-sm"
version = "0.1.0"
description = "Convert StepMania DWI simfiles to the SM format."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
package = false
